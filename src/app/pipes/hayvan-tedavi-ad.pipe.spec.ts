import { HayvanTedaviAdPipe } from './hayvan-tedavi-ad.pipe';

describe('HayvanTedaviAdPipe', () => {
  it('create an instance', () => {
    const pipe = new HayvanTedaviAdPipe();
    expect(pipe).toBeTruthy();
  });
});
