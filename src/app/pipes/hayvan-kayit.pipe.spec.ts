import { HayvanKayitPipe } from './hayvan-kayit.pipe';

describe('HayvanKayitPipe', () => {
  it('create an instance', () => {
    const pipe = new HayvanKayitPipe();
    expect(pipe).toBeTruthy();
  });
});
